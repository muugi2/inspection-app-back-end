import 'package:flutter/material.dart';

class InstallPage extends StatelessWidget {
  const InstallPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Суурьлуулалт'));
  }
}
